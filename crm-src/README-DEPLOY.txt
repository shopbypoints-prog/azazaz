Paraveda CRM v3.66 — نشر على الهوست
================================================================

⚠️ الهوت فكس: التاريخ ديال الطلبيات كان كيرجع بوحدو لتاريخ الشهر — تصلح فـ v3.66.

شنو تبدل فـ v3.66 (القفل ديال التواريخ 🔒)
----------------------------------------------------------------
1) index.html:
   - التاريخ كيتقفل بالختم وقت ميلاد الطلبية، وقت الاستيراد، ووقت استرجاع الباكاب.
   - إصلاح السباق فالمتصفح: التعديل ديالك (حتى إلا جا Sync فنفس اللحظة) ما كيضيعش.
2) api.php (ضروري):
   - قفل صلب فالسيرفر: التواريخ (dateCreation / dateConfirmation / dateExp / dateLiv)
     ما كيتبدلوش غير بتعدیل بشري بختم أحدث. متصفح فيه نسخة قديمة ولا كاش قديم
     ما يقدرش يبدل التواريخ بعد اليوم — القيمة المخزنة هي اللي كتربح.
   - الساعات المتقدمة (جهاز وقته غالط) محجوبة: الأختام المستقبلية كيتقصيو لوقت السيرفر.
   - كل تبديل تاريخ كيتسجل فـ crm-paraveda-data/audit.log (date-change).

نشر (3 ملفات)
----------------------------------------------------------------
1) public_html/index.html   → فـ public_html/ (بدّل القديم)
2) public_html/api.php      → فـ public_html/ (بدّل القديم — v3.66 ضروري)
3) crm_data.json ما كيتبدلش — خلي القديم فـ بلاصتو

⚠️⚠️ بعد النشر: كل جهاز كيستعمل CRM خاصو يدير Ctrl+F5 (ولا يمسح الكاش)!
الأجهزة اللي فيها نسخة قديمة من الصفحة هي السبب الرئيسي فتبديل التواريخ.
دخول الأدمين: admin@paraveda.ma

الباكاب الأوتوماتيكي — فين كاين؟
----------------------------------------------------------------
1) فالسيرفر: مجلد crm-paraveda-data/backups/
   - b-*.json  = نسخة قبل كل كتابة (آخر 30)
   - d-*.json  = نسخة يومية (آخر 30 يوم)
   → للاسترجاع: حمل أحدث نسخة قبل المشكل، وبدل crm_data.json بيها
     (ولا عيط ليا نديرو ليك).
2) فالمتصفح: localStorage كيسجل أوتوماتيك آخر نسخة فـ paraveda_backup_v1.
3) audit.log: غادي تلقى فيه سطور date-change كتبين التاريخ القديم والجديد ديال كل طلبية.
