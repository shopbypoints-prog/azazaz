Paraveda CRM v3.67 — نشر على الهوست
================================================================

⚠️⚠️ هاد النسخة كتصلح جوج مشاكل كبار:
  1) التاريخ ديال الطلبيات كان كيرجع بوحدو
  2) الطلبيات كيتمسحوا بوحدهم (من عند اليوزر ومن عند الأدمين)

شنو تبدل فـ v3.67
----------------------------------------------------------------
✦ المتصفح (index.html):
  - إصلاح الثغرة القاتلة: الختم ديال المزامنة كان كيتسجل قبل ما ينجح الرفع
    → إلا الرفع كيفشل، الـ pull كيمحي الطلبية من المتصفح → كتمسح نهائيا.
    دابا: الختم كيتسجل غير بعد نجاح الرفع.
  - نافذة أمان 15 دقيقة: أي طلبية ما وصلتش للسيرفر عمرها ما كتمسح من المتصفح.
  - الطلبيات الجديدة كتختم من لحظة الميلاد/الاستيراد/الاسترجاع.
  - إنذار أحمر إلا ساعة الجهاز غالطة (الساعة الغالطة كانت كتسبب مسح صامت).

✦ السيرفر (api.php):
  - JOURNAL (دفتر الكتابات): كل كتابة ناجحة كتسجل. إلا crm_data.json تبدل
    يدويا (FTP) ولا تخسر → أحدث حالة كترجع بوحدها. مستحيل عاد نخسرو بيانات.
  - مرايا: كاين جوج بلاصات ديال crm_data.json → كيتكتبو بجوج ديما ومتطابقين.
  - الأجهزة اللي ساعتهم متأخرة: الطلبيات الجديدة ديالهم ما بقاتش كتمسح بصمت.
  - قفل التواريخ (من v3.66): التاريخ ما كيتبدل غير بتعدیل بشري مؤكد.

نشر (غير 2 ملفات — crm_data.json ما كيتبدلش!)
----------------------------------------------------------------
1) public_html/index.html   → فـ public_html/ (بدّل القديم)
2) public_html/api.php      → فـ public_html/ (بدّل القديم — v3.67 ضروري)
3) ⚠️ خلي crm_data.json القديم فـ بلاصتو (فيه البيانات الحقيقية)

⚠️⚠️ مهم بزاف: كل جهاز كيستعمل CRM خاصو يدير Ctrl+F5 مرة وحدة على الأقل!
الأجهزة اللي فيها نسخة قديمة هي مصدر أغلب المشاكل.

الباكاب الأوتوماتيكي — فين كاين؟
----------------------------------------------------------------
1) crm-paraveda-data/backups/:
   - b-*.json = نسخة قبل كل كتابة (آخر 30)
   - d-*.json = نسخة يومية (آخر 30 يوم)
2) crm-paraveda-data/journal.log = دفر الكتابات (حماية إضافية)
3) crm-paraveda-data/audit.log:
   - سطور date-change = كل تبديل تاريخ (القديم → الجديد)
   - سطور ghost/corrupt/recover = أي حاجة غير عادية
4) فالمتصفح: نسخة أوتوماتيك فـ localStorage (paraveda_backup_v1)

الاختبارات
----------------------------------------------------------------
tests/date-lock.test.js   → 15 سيناريو (التاريخ ما كيترجعش)  ✓
tests/orders-loss.test.js → 12 سيناريو (الطلبيات ما كيتمسحوش) ✓
دخول الأدمين: admin@paraveda.ma
